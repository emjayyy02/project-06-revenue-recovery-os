# Build workflow

Build future screens directly in code, following the established Customer 360, Dashboard, and Analytics implementations identified in PRODUCT.md.

The default is stored in `.impeccable/config.json` as `buildPath: code`. Create a mockup first only when a future screen requires a major new layout or visual direction. This exception does not change the stored default.
